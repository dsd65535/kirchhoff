[project]
name = "lti"
version = "0.1.0"
description = "A lightweight circuit simulator for LTI systems"
readme = "README.md"
authors = [
    { name = "Dimitar Dimitrov", email = "dimitar.dimitrov@cba.mit.edu" }
]
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.10.5,<0.11.0"]
build-backend = "uv_build"
